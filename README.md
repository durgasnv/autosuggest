# autosuggest

Session 1:
We will be developing and deploying an autosuggest website.

First , we are trying to understand the big picture on how the web server works.

git command :
git add -A
git add .
git commit -m "some message"
git push
git pull
git fetch
